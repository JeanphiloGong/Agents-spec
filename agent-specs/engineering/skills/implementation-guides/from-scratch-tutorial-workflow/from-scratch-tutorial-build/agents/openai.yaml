interface:
  display_name: "From-Scratch Tutorial Build"
  short_description: "v0.1.1 - Build a connected from-scratch implementation tutorial"
  default_prompt: "Use $from-scratch-tutorial-build to produce a complete markdown from-scratch implementation tutorial. Derive behavior and invariants into connected code versions; every step must Add/Replace, Step Check, Verify, Freeze This Version, and Still Lacks, with final complete code coming from the last connected step."
