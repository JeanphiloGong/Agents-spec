interface:
  display_name: "Reference Core Teaching Plan"
  short_description: "v0.1.5 - Shape a selected chain into a nano asset"
  default_prompt: "Use $reference-core-teaching-plan to turn an already selected core chain into a source-independent real-world zero, an engineering zero that excludes the current-chain capability, a base/current split, module lineage gate, one asset version checkpoint, blocking prerequisite questions when needed, and a standalone nano asset promise. Treat blog/tutorial/personal-kb framing as a knowledge-asset lens for decomposition, reader value, prerequisite split, version boundary, README pressure, and validation pressure; do not write the final article or tutorial. Produce concrete constraints for $reference-core-plan before building the learning module."
