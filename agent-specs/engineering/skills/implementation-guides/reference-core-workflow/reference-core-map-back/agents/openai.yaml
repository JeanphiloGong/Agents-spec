interface:
  display_name: "Reference Core Map Back"
  short_description: "v0.1.2 - Map learning modules back to production"
  default_prompt: "Use $reference-core-map-back to convert a validated reference-core learning module into concrete production modules, boundaries, architecture translation, first landing tests, module-to-production mappings, and a handoff to $human-led-main-landing-skill without writing the production patch."
