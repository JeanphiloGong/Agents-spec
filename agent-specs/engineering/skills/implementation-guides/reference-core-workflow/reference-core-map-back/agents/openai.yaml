interface:
  display_name: "Reference Core Map Back"
  short_description: "v0.1.4 - Map nano assets back to production"
  default_prompt: "Use $reference-core-map-back to convert a validated reference-core nano asset and its version checkpoint evidence into concrete production modules, boundaries, architecture translation, first landing tests, module-to-production mappings, and a handoff to $human-led-main-landing-skill without writing the production patch."
