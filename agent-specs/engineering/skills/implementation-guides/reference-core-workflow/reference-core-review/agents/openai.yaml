interface:
  display_name: "Reference Core Review"
  short_description: "v0.1.5 - Review reference-core learning modules"
  default_prompt: "Use $reference-core-review to check whether a reference-core learning module is independently understandable without source-project context, runnable, chain-complete, invariant-preserving, scenario-fit in src architecture, git-reviewable, safely placed outside production imports, version-checkpoint ready, tag-worthy when appropriate, and optionally ready for map-back."
