interface:
  display_name: "Reference Core Plan"
  short_description: "v0.1.8 - Plan a reference-core learning module"
  default_prompt: "Use $reference-core-plan to plan exactly one standalone runnable learning-module checkpoint by carrying forward any $reference-core-teaching-plan source-independent zero point, base/current split, lineage gate, version checkpoint, and teaching constraints, stopping or switching to prerequisite work when the selected chain is blocked, then naming the extracted chain, defining invariant, scenario-fit src architecture, module layout, included and deferred learning boundaries, safe placement, validation checks, tag recommendation, optional source notes, and build handoff before writing module code."
