interface:
  display_name: "Reference Core Plan"
  short_description: "v0.1.9 - Plan a reference-core nano asset"
  default_prompt: "Use $reference-core-plan to plan exactly one standalone runnable nano/reference learning-asset checkpoint by carrying forward any $reference-core-teaching-plan source-independent zero point, base/current split, lineage gate, version checkpoint, knowledge-asset lens, and module constraints, stopping or switching to prerequisite work when the selected chain is blocked, then naming the extracted chain, defining invariant, scenario-fit src architecture, module layout, included and deferred learning boundaries, safe placement, validation checks, nano asset readiness, from-scratch tutorial handoff, tag recommendation, optional source notes, and build handoff before writing module code."
