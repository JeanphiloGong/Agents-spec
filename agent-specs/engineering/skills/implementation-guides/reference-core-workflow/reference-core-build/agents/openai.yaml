interface:
  display_name: "Reference Core Build"
  short_description: "v0.1.0 - Build a runnable minimal-complete reference sample"
  default_prompt: "Use $reference-core-build to turn an approved reference-core plan into a runnable minimal-complete sample with safe placement, included/deferred boundaries, a happy path, a boundary or failure check, and no production integration patch."
