interface:
  display_name: "Workflow Build"
  short_description: "v0.1.2 - Sketch invariants, implement directly, then simplify"
  default_prompt: "Use $workflow-build to sketch the next slice's control flow and invariants, implement it directly first, then simplify only with evidence."
