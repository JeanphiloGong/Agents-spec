interface:
  display_name: "Workflow Build"
  short_description: "v0.1.0 - Implement one coherent slice at a time"
  default_prompt: "Use $workflow-build to implement the next small, testable slice without expanding scope."
