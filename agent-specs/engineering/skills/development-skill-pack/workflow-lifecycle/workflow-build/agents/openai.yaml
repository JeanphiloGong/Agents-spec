interface:
  display_name: "Workflow Build"
  short_description: "v0.1.6 - Gate builds with hard skeleton checkpoints"
  default_prompt: "Use $workflow-build to scope the slice, create coverage-mapped skeleton gates for non-trivial targets, show the skeleton diff, implement directly, run a coverage drift check, then audit speculative helpers."
