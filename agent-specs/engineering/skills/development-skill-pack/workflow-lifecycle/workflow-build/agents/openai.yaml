interface:
  display_name: "Workflow Build"
  short_description: "v0.1.1 - Implement one coherent slice at a time from a sketch when present"
  default_prompt: "Use $workflow-build to implement the next small, testable slice without expanding scope; when a .agent-runs sketch exists, read it first and write build-log.md."
