interface:
  display_name: "Workflow Build"
  short_description: "v0.1.3 - Create code-local skeletons before direct implementation"
  default_prompt: "Use $workflow-build to create a code-local skeleton for the next non-trivial slice, implement it directly first, then simplify only with evidence."
