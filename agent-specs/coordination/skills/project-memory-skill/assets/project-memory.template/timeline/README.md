# Timeline Indexes

Use monthly index pages when the project generates enough memory that a date-only
directory scan becomes noisy.

Suggested file pattern:

- `YYYY-MM.md`

Each monthly page should link the notable session and decision records created in
that month.
