interface:
  display_name: "Project Doc Record"
  short_description: "v0.4.6 - Record one doc with topic-family placement"
  default_prompt: "Use $project-doc-record-skill to inspect this repository's nearby docs, tests tree, applicable architecture or lifecycle guidance, root README, docs/README, and code ownership, determine the real code-owned ownership node and lowest common ancestor, distinguish runtime ownership from test-suite ownership before writing, decide whether this wave needs one standalone artifact or a node-local topic-family container, choose the correct page role, keep repo and node READMEs focused on purpose, boundaries, and main flow, keep docs landing pages focused on navigation, preserve child detail instead of collapsing it into a parent doc, and add only the minimum parent, topic-family, neighbor, or follow-up links needed for local discoverability."
