interface:
  display_name: "Project Doc Record"
  short_description: "v0.4.3 - Record one doc without collapsing repo landing into docs landing"
  default_prompt: "Use $project-doc-record-skill to inspect this repository's nearby docs, applicable architecture or lifecycle guidance, root README, docs/README, and code ownership, determine the real code-owned ownership node and lowest common ancestor, avoid treating docs-only grouping folders as nodes by default, keep root README as a lightweight repository landing page, choose the correct artifact, preserve child detail instead of collapsing it into a parent doc, and write the resulting document with the minimum parent, neighbor, or follow-up links needed for local discoverability."
